const images = document.querySelectorAll(".image");

if (images) {
  images.forEach(image => {
    image.onmouseover = () => {
      document.querySelector(".active").classList.remove("active");
      image.classList.add("active")
    }
  })
}

// banner
// onmouseover khi hover chuoojt