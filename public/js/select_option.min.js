
 //select thứ hai
    // bind change event to select
    $('#dynamic_select').on('change',function(){
        var url = $(this).val();
        if (url){ 
            window.location = url; 
        }
        return false;
    });
    current_chapter1();
  function current_chapter1(){
    var url = window.location.href;
    $('#dynamic_select').find('option[value="'+url+'"]').attr("selected",true);
  }


//select thứ 1

    // bind change event to select
    $('#dynamic_select1').on('change',function () {
        var url = $(this).val(); // get selected value
        if (url) { // require a URL
            window.location = url; // redirect
        }
        return false;
    });
    current_chapter();

function current_chapter(){
  var url = window.location.href;
  $('#dynamic_select1').find('option[value="'+url+'"]').attr("selected",true);
}

